//Allows us to write the code in newer version of JS; optional
"use strict";

/*
Data Types:
string -> "Malaika"
number -> 12 (Ranges till 2 to the power 53)
bigint -> For extremely large numbers
boolean -> true/false
null -> nothing
undefined -> when value is not assigned to the variable
symbol -> For making a value unique
*/

//We have assigned a valid data type to name variable
let name = null;

//Case for undefined i.e. when no value is initialized
let email;

console.log(typeof null); //Object
console.log(typeof undefined); //Undefined
console.log(email); //Undefined
